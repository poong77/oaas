/**
 * help.oapms.com → 통합AS 아티클 이관 스펙 (PMS · 객실관리).
 *
 * 1게시물 → 의도별(howto/feature/troubleshoot) 다수 아티클로 분할.
 * 이미지는 source.imgs 배열의 인덱스로 참조 — import 시 sharp composite로
 * 브라우저 프레임 + sunset 배경 입혀 Vercel Blob에 업로드된 후, buildBody가
 * 합성된 Blob URL을 받아 본문에 삽입.
 *
 * source.hash: help.oapms.com 원본 아티클 URL의 8자 hash. Blob path 디렉토리.
 */

const IMG_BASE = 'https://cf.channel.io/document/spaces/15320/usermedia';
const A = (p) => `${IMG_BASE}/${p}`;

// ─────────────────────────────────────────────────────────────
// 원본 게시물별 이미지 source 정의 (Blob composite 대상)
// ─────────────────────────────────────────────────────────────
export const sources = [
  // 기존 import 된 게시물 — 이미지만 재합성 (article은 title 기준 idempotent skip)
  {
    hash: 'b02884a7',
    label: '실시간 객실(오늘)',
    imgs: [
      A('692fe6c9c6ac424fd610'),
      A('69128a7f102598c92e4d'),
      A('69128a74b37176b24b59'),
    ],
  },
  {
    hash: '076ed8e4',
    label: '객실현황(이달)',
    imgs: [
      A('69129682e0847c414d35'),
      A('6913e93953431bc69dfa'),
      A('6913eb3f1444d2cad8ac'),
    ],
  },
  {
    hash: 'be8de2e9',
    label: '예약등록',
    imgs: [
      A('692fe6dbabc2c413a403'),
      A('6913ece6d6fb1e699e6c'),
      A('6913ed0f50980913b547'),
      'https://cf.channel.io/document/spaces/15320/articles/469023/revisions/745070/usermedia/6913f354c4630ebaf079',
      A('6913f3a02f89876c18e5'),
      A('69140d566e0e65906c90'),
      A('69140e20ca4e58d4b1de'),
      A('691419e97a2c60dbc889'),
      A('69141a6a8b8aa7a29063'),
      A('691526840dd2da7ba748'),
      A('691537ac4e57df894d07'),
      'https://cf.channel.io/document/spaces/15320/articles/470264/revisions/746794/usermedia/6915380b94209e6a0866',
    ],
  },
  {
    hash: '5d98c1a9',
    label: '예약조회',
    imgs: [
      A('692fe6ed039bb2584ed1'),
      A('691545a274dd82cb151c'),
      A('691545d1a09b04ef033a'),
    ],
  },
  {
    hash: '5d4e96c9',
    label: '예약 캘린더',
    imgs: [
      A('692fe6f5a8fa539cfe4f'),
      A('69154763d1d148dc22e3'),
      A('691548103fa904009f10'),
    ],
  },
  {
    hash: 'cac9930d',
    label: '체크인/아웃',
    imgs: [
      A('692ff5b4dcfb7584a313'),
      A('69155a0fbf3f60a264d4'),
      A('69155a5d9bf444894388'),
      A('691bc0e6e1ca47e43f5f'),
      A('691bc0f2ee8584a9e9ec'),
      A('691bc96d211a7a657ecb'),
      'https://cf.channel.io/document/spaces/15320/articles/475372/revisions/758850/usermedia/691bca3d793cfa246e85',
      'https://cf.channel.io/document/spaces/15320/articles/475372/revisions/758850/usermedia/691bcaa69e66252a17ea',
      A('691bcac2100b2832d108'),
    ],
  },
];

// ─────────────────────────────────────────────────────────────
// 13개 article spec (sources[].hash로 이미지 참조)
// ─────────────────────────────────────────────────────────────
export const specs = [
  // ───── 기존 3건 (실시간 객실(오늘)) — title 기준 idempotent skip ─────
  // 이미지만 새 frame 입혀서 같은 Blob path에 덮어씀 → 본문 URL 그대로 유효
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '실시간 객실(오늘)'],
    title: '실시간 객실(오늘) 화면 구성',
    summary:
      'PMS 메인 운영 화면. 당일 객실을 필터·카드형으로 한눈에 보고, 11종 객실 상태값 의미를 정리합니다.',
    keywords: ['실시간객실', '객실상태', '화면구성', '필터', '카드뷰'],
    sourceHash: 'b02884a7',
    buildBody: (i) => `## 개요

실시간 객실(오늘)은 PMS에서 당일 객실 상태를 한 화면으로 파악하는 메인 운영 화면입니다.
프론트 데스크 운영자가 가장 자주 보는 화면으로, 필터와 카드형 표시를 통해 호실·현 상태·투숙기간·투숙자명을 즉시 확인할 수 있습니다.

![화면 구성](${i[0]})

## 위치(메뉴 경로)

PMS > 객실관리 > 실시간 객실(오늘)

## 항목 설명

### 상단 검색 필터

| 항목 | 설명 |
|---|---|
| 객실 상태 | 11종 상태로 필터 (공실/재실/대실/예약확정 등) |
| 객실 타입 | 숙소에 설정된 타입별 선별 조회 |
| 객실 층 | 층별 객실 조회 |
| 객실 수 | 현재 필터가 적용된 객실 수 총합 |

### 하단 카드형 객실정보

| 항목 | 설명 |
|---|---|
| 호실 | 객실 번호 |
| 현 상태 | 11종 상태 중 현재 상태 |
| 투숙기간 | 체크인 ~ 체크아웃 일자 |
| 투숙자명 | 대표 게스트명 |

### 객실 상태 11종

| 상태 | 의미 |
|---|---|
| 공실 | 비어있는 객실 |
| 재실 | 투숙중인 객실 |
| 대실 | 시간제로 사용중인 객실 |
| 예약확정 | 예약이 확정된 경우 |
| 예약보류 | 재고에 포함되는 상태 |
| 예약대기 | 재고에 포함되지 않는 상태 |
| 퇴실예정 | 금일 체크아웃 예정 |
| 퇴실요청 | 폰키로 고객이 체크아웃 버튼 클릭 |
| 청소요청 | 체크아웃 후 청소 필요 (=Dirty) |
| 청소요청(폰키) | 폰키로 고객이 청소요청 버튼 클릭 |
| O.O.O | 객실정비 등 판매 중지 |

![객실 상태 색상](${i[1]})

## 관련 문서

- 실시간 객실(오늘)에서 객실 상태 빠르게 변경하기 (howto)
- 청소요청(폰키) 상태값이 화면에 안 보일 때 (troubleshoot)
- 예약등록 — 카드 클릭 시 이동
- 체크인/아웃 — 재실 카드 클릭 시 이동
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '실시간 객실(오늘)'],
    title: '실시간 객실(오늘)에서 객실 상태 빠르게 변경하기',
    summary:
      '우클릭/좌클릭으로 객실 상태를 즉시 변경하고, 카드 클릭으로 예약등록·체크인 화면으로 이동하는 방법입니다.',
    keywords: ['실시간객실', '상태변경', '우클릭', '청소요청', '빠른변경'],
    sourceHash: 'b02884a7',
    buildBody: (i) => `## 목표

실시간 객실(오늘) 화면에서 마우스 클릭만으로 객실 상태를 즉시 변경하고, 후속 화면(예약등록·체크인/아웃)으로 빠르게 이동합니다.

## 사전 준비

- 메뉴 경로: PMS > 객실관리 > 실시간 객실(오늘)
- 화면이 정상 로드되어 객실 카드가 보이는 상태

## 단계

1. **전체 현황 확인** — 상단 필터를 모두 풀어 전체 객실을 카드 형태로 띄웁니다.

2. **객실 상태 빠른 변경** — 카드 위에서 우클릭 또는 좌클릭으로 즉시 상태를 변경합니다.

   | 현재 상태 | 클릭 | 변경 후 |
   |---|---|---|
   | 재실 | 우클릭 | 청소요청/재실 |
   | 체크아웃(Dirty) | 우클릭 | 공실(Clean) |
   | 청소요청 | 좌클릭 | 공실(Clean) |
   | 청소요청 | 우클릭 | 점검(Inspect) |

3. **공실 카드 클릭** — \`예약등록\` 화면으로 이동합니다.

4. **재실 카드 클릭** — \`체크인/아웃\` 화면으로 이동합니다.

![사용 예시](${i[2]})

## 다음 단계

- 새 예약을 받았다면 → \`예약등록\`
- 체크아웃을 처리한 객실 일괄 마감은 → \`객실 일마감 > 일마감 사전체크\`
- 11종 상태값 의미가 헷갈리면 → \`실시간 객실(오늘) 화면 구성\` 참고
`,
  },
  {
    productCode: 'pms',
    contentType: 'troubleshoot',
    categoryPath: ['객실관리', '실시간 객실(오늘)'],
    title: '청소요청(폰키) 상태값이 화면에 안 보일 때',
    summary:
      '청소요청(폰키)는 키리스(폰키) 사용 숙소에만 표시됩니다. 미사용 숙소는 정상 동작이며 별도 조치 불필요.',
    keywords: ['폰키', '키리스', '청소요청', '상태값', '누락'],
    sourceHash: 'b02884a7',
    buildBody: () => `## 증상

실시간 객실(오늘) 화면에서 다른 숙소에는 보이는 **"청소요청(폰키)"** 상태값이 우리 숙소 화면에는 나타나지 않습니다. 필터에도 선택지로 보이지 않을 수 있습니다.

## 원인

1. **키리스(폰키) 프로그램 미사용** — 청소요청(폰키)는 키리스 모듈을 사용하는 숙소에만 자동 노출되는 상태값입니다. 키리스가 비활성이면 해당 상태가 발생하지 않으므로 화면 표기에서도 제외됩니다.

## 해결 단계

1. **키리스 도입 여부 확인** — 어드민의 모듈 설정에서 키리스(폰키)가 활성화돼 있는지 확인합니다.

2. **비활성 = 정상** — 키리스를 사용하지 않는 숙소라면 청소요청(폰키)가 안 보이는 것이 정상 동작입니다. 별도 조치 불필요.

3. **일반 청소요청은 정상 표시 확인** — 키리스 사용 여부와 무관하게 일반 "청소요청" 상태값은 동일하게 표시됩니다. 이게 안 보이면 별도 이슈로 봐야 합니다.

## 그래도 안 되면

- 키리스를 사용 중인데도 청소요청(폰키)가 안 보이는 경우 → AS 접수로 문의 (스크린샷 + 호텔 식별자 첨부)
- 키리스 도입을 검토 중이라면 → \`Keyless > OA 키리스\` 카테고리 참고
- 일반 "청소요청" 자체가 표시되지 않는 경우는 별도 트러블슈팅 필요
`,
  },

  // ───── 신규 1) 객실현황(이달) — 2건 ─────
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '객실현황(이달)'],
    title: '객실현황(이달) 화면 구성',
    summary:
      '월 단위로 객실 상태와 예약 현황을 캘린더 뷰로 확인하는 화면. 예약 상태값 6종·재고·OTA 결제 구분을 정리합니다.',
    keywords: ['객실현황', '캘린더', '예약상태', '재고', 'OTA'],
    sourceHash: '076ed8e4',
    buildBody: (i) => `## 개요

객실현황(이달)은 PMS에서 최대 1개월 단위로 객실 상태와 예약 현황을 캘린더 뷰로 확인하는 화면입니다.
프론트·매니저가 가동률·재고·결제 유형을 한눈에 파악하고, 빈 칸을 클릭해 예약을 생성하거나 객실을 배정할 수 있습니다.

![화면 구성](${i[0]})

## 위치(메뉴 경로)

PMS > 객실관리 > 객실현황(이달)

## 항목 설명

### 예약 상태값 6종

| 상태 | 의미 |
|---|---|
| 예약확정 | 예약 생성 시 최초 상태 |
| 예약보류 | 재고에 포함되는 상태 |
| 예약대기 | 재고에 포함되지 않는 상태 |
| 체크인 | 체크인 완료, 재실 중 |
| 퇴실예정 | 당일 체크아웃 예정 (영업일 기준) |
| O.O.O | 판매 중지된 객실 상태 |

### 재고 표시

![재고 표시](${i[1]})

- 일자별 체크인 수, 예약 건수, 객실재고 수 표시
- 날짜 클릭 시 선택한 일자의 예약 정보 확인
- **삼각 표시**는 객실 번호 미배정 내역이 있을 때 표시

### OTA 선결제/현장결제

![OTA 결제 구분](${i[2]})

| 구분 | 표기 색 |
|---|---|
| 선결제 예약 | 흰색 |
| 현장결제 예약 | 노란색 |

색상 표기가 안 보이는 경우 AS 접수로 요청하면 적용 가능합니다.

## 관련 문서

- 객실현황(이달)에서 일자 이동·예약 생성·청소 상태 관리하기 (howto)
- 실시간 객실(오늘) 화면 구성 (feature)
- 예약등록 — 빈 칸 클릭 시 이동
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '객실현황(이달)'],
    title: '객실현황(이달)에서 일자 이동·예약 생성·청소 상태 관리하기',
    summary:
      '캘린더 일자 이동, 빈 칸 클릭으로 예약 생성, 객실 우클릭으로 Clean·Dirty 전환하는 단축 동선입니다.',
    keywords: ['일자이동', '예약생성', '청소상태', '객실배정', '드래그'],
    sourceHash: '076ed8e4',
    buildBody: (i) => `## 목표

객실현황(이달) 화면에서 마우스 클릭·드래그만으로 일자 이동, 신규 예약 생성, 청소 상태 전환을 빠르게 처리합니다.

## 사전 준비

- 메뉴 경로: PMS > 객실관리 > 객실현황(이달)
- 객실 캘린더가 정상 로드된 상태

## 단계

1. **일자 이동** — 캘린더 상단에서 원하는 단위로 이동합니다.

   | 동작 | 결과 |
   |---|---|
   | \`<\`, \`>\` 클릭 | 하루 단위 앞/뒤 이동 |
   | \`다음달\` 클릭 | 한달 단위 이동 |
   | \`일자\` 클릭 | 캘린더로 원하는 일자 직접 이동 |
   | \`2주\`/\`3주\`/\`1달\` 클릭 | 해당 기간의 예약 표기 |
   | \`TODAY\` 클릭 | 당일(영업일) 체크인/체크아웃 확인 |

2. **빈 칸 클릭으로 예약 생성** — 빈 칸을 클릭하면 \`예약등록\` 화면으로 이동합니다.
   - 여러 일자를 **드래그**하면 2박 이상 예약 건을 한 번에 생성할 수 있습니다.

3. **삼각 표시로 미배정 객실 처리** — 삼각 표시가 있는 일자는 객실 번호 미배정 예약이 있다는 뜻입니다. 해당 일자를 눌러 상세 보기 후 객실을 배정합니다.

![재고 표시](${i[1]})

4. **객실 우클릭으로 청소 상태 전환** — 객실타입/호수를 우클릭하면 Clean ↔ Dirty 전환이 가능합니다.

## 다음 단계

- 새 예약 등록 화면에서 상세 정보 입력 → \`예약등록\` 참고
- 체크인은 \`실시간 객실(오늘)\`에서 공실 카드 클릭으로도 진입 가능
- 객실 매출/입금 처리 후 마감 → \`객실 일마감 > 일마감 사전체크\`
`,
  },

  // ───── 신규 2) 예약등록 — 4건 ─────
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '예약등록'],
    title: '예약등록 화면과 5대 기능 개요',
    summary:
      '예약 생성·요금 설정·조식·단체예약·예약금 등 PMS 예약 운영의 모든 핵심을 다루는 메인 화면입니다.',
    keywords: ['예약등록', '단체예약', '예약금', '조식', '거래처'],
    sourceHash: 'be8de2e9',
    buildBody: (i) => `## 개요

예약등록은 PMS에서 신규 예약을 생성하거나 기존 예약을 수정하는 메인 화면입니다.
개별·단체 예약, 일별 요금 설정, 조식 관리, 예약금 등록까지 한 화면에서 처리하며 거래처·고객 마스터와 연계됩니다.

![화면 구성](${i[1]})

## 위치(메뉴 경로)

PMS > 객실관리 > 예약등록

## 항목 설명

| 영역 | 구성 |
|---|---|
| 상단 | 저장·체크인·예약금 등 버튼 모음 |
| 중간 | 예약정보 / 연락처 / 거래처 정보 |
| 하단 | \`객실정보\` 탭 / \`예약금\` 탭 |

### 5대 핵심 기능

| # | 기능 | 요약 |
|---|---|---|
| 1 | 예약 생성 | 그룹명·거래처·고객·일자·객실타입·요금 입력 후 저장 |
| 2 | 일별 요금 설정 | 일자별 할인율(D/C) 또는 금액 직접 입력. 총 요금 자동 안분 |
| 3 | 무료 객실(Complimentary) | CompReason 선택 시 해당 일자 0원 처리 |
| 4 | 조식 설정 | 예약 저장 후 일자별 인원/금액 수정. 객실식사인원조회에서 일괄 확인 |
| 5 | 단체 예약 | 객실 수 2개 이상 입력 후 객실 자동/수동 배정 |

### 식별자 구분

- **ACCT#** — PMS가 부여하는 고유 예약번호
- **OTA 예약번호** — 판매채널(OTA)에서 부여한 외부 번호

## 관련 문서

- 예약 생성·일별 요금·조식·단체 예약하기 (howto)
- 예약금 등록·처리하기 (howto)
- 단체 예약 마스터 객실 미설정 시 (troubleshoot)
- 예약조회 — 등록한 예약 검색·체크인
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '예약등록'],
    title: '예약 생성·일별 요금·조식·단체 예약하기',
    summary:
      '신규 예약 입력부터 일별 요금/무료 제공/조식 설정, 단체 예약 객실 배정까지 한 번에 끝내는 절차입니다.',
    keywords: ['예약생성', '일별요금', '조식', '단체예약', '객실배정'],
    sourceHash: 'be8de2e9',
    buildBody: (i) => `## 목표

신규 예약을 생성하고 일별 요금·무료 제공·조식·단체 예약(객실 배정)까지 한 번에 마칩니다.

## 사전 준비

- 거래처 마스터에 사용할 거래처가 등록돼 있을 것 (\`설정 > 공통설정 > 거래처 설정\`)
- 메뉴 경로: PMS > 객실관리 > 예약등록

## 단계

1. **예약 생성** — 기본 정보를 입력하고 저장합니다.

   - \`그룹명\` — 보통 투숙객/단체 이름
   - \`거래처\` — 판매채널·여행사·제휴회사
   - \`고객명\`, \`인원수\`, \`일자\`, \`객실타입\`, \`요금\`
   - **저장** 클릭 → ACCT# 자동 생성 → 실시간 객실상태·객실현황·예약일정표에 \`예약확정\`으로 표시

   ![예약 생성 화면](${i[3]})

   > 기존 고객을 등록하려면 돋보기 클릭 후 조회해 불러옵니다 (![고객 조회](${i[4]})).

2. **일별 요금 설정** — \`일별 요금\` 버튼 클릭 → 수정 창에서 일자별로 입력합니다.

   - \`D/C\` 칸에 할인율 또는 \`객실요금\` 칸에 받을 금액
   - 여러 일자가 있으면 \`총 요금 입력\` 칸에 전체 금액 → 박수만큼 자동 안분
   - \`확인\` 클릭 → 우측 하단 Total 확인 → 상단 \`저장\`

   ![일별 요금](${i[5]})

3. **무료 객실(Complimentary) 설정** — 일별요금 수정 창의 \`CompReason\` 칸에서 사유 선택 → 해당 일자 0원 표시 (![무료 객실](${i[6]})).

4. **조식 설정** — 예약 저장(ACCT# 생성) 후 가능합니다.

   - 조식 칸 돋보기 클릭 → 일자/조식Adult/조식Child 인원·금액 수정
   - 등록된 식사 인원은 \`객실식사인원조회\`에서 일괄 조회 가능

   ![조식 설정](${i[7]})

5. **단체 예약(객실 2개 이상)** — \`객실정보\` 탭에서 객실 수를 2개 이상 입력 → 저장 → 객실 칸 돋보기로 배정합니다.

   - 자동 배정: 상단 \`객실배정\` 버튼
   - 수동 배정: \`Room#\` 칸에 직접 객실 선택
   - 상단 \`확인\` → 저장

   ![단체 예약 객실배정](${i[8]})

## 다음 단계

- 예약금을 받았다면 → \`예약금 등록·처리하기\` (howto)
- 단체 예약 마감이 객실별로 분리되어 보인다면 → \`단체 예약 마스터 객실 미설정 시\` (troubleshoot)
- 등록된 예약을 검색하려면 → \`예약조회\`

## 팁 — 예약을 등록하는 3가지 진입 경로

1. \`객실현황(이달)\`에서 예약할 객실·날짜 클릭
2. \`예약등록\` 메뉴에서 정보 입력 후 저장
3. \`실시간 객실(오늘)\`에서 \`공실\` 카드 클릭
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '예약등록'],
    title: '예약금 등록·처리하기',
    summary:
      '사전 결제 예약금을 등록하고, 체크인 시 선수금으로 자동 대체 처리한 뒤 정산에 사용하는 흐름입니다.',
    keywords: ['예약금', '선수금', '체크인', '입출금', '대체처리'],
    sourceHash: 'be8de2e9',
    buildBody: (i) => `## 목표

고객 사전 결제 예약금을 PMS에 등록하고, 체크인 시점에 선수금으로 자동 대체 처리해 정산에 사용합니다.

## 사전 준비

- \`예약확정\` 상태의 예약 건이 존재
- 메뉴 경로: PMS > 객실관리 > 예약등록

## 단계

1. **예약금 등록** — 예약확정 건의 \`예약금\` 탭을 클릭합니다.

   - 상단 \`예약금\` 버튼 → 예약금 입금관리 창
   - **구분** 선택: CARD / CASH / 대내후불 / 대외후불 / 선수금 / 예금
   - **금액** 입력 → \`저장\`
   - 입력된 내역 확인

   ![예약금 탭](${i[9]})

   ![입금관리](${i[10]})

2. **체크인 시 선수금 대체 처리** — 예약 건을 체크인 처리하면 자동 알림이 뜹니다.

   - \`예약금이 있습니다. 선수금으로 대체 처리하시겠습니까?\` → **Yes**
   - 자동으로 선수금 내역이 생성됨 (예약금과 동일 금액)

   ![선수금 대체 처리](${i[11]})

3. **입출금 확인** — 입출금 화면에서 자동 생성된 선수금 내역을 확인합니다.

4. **체크아웃 정산에 사용** — 체크아웃 시 객실 요금 정산에서 선수금이 자동 차감됩니다.

## 다음 단계

- 체크인 진행 → \`체크인/아웃 처리하기\` (howto)
- 매출/입금 상세 관리 → \`체크인/아웃\` 화면의 입출금 탭

## 주의사항

선수금 대체 처리 후 **수기로 같은 금액을 다시 입금하면 중복 입금이 발생**합니다.
체크인 시 사전 등록된 예약금이 있는지 먼저 확인한 뒤 입금 처리를 진행하세요.
`,
  },
  {
    productCode: 'pms',
    contentType: 'troubleshoot',
    categoryPath: ['객실관리', '예약등록'],
    title: '단체 예약 마감이 객실별로 분리될 때 (마스터 객실 미설정)',
    summary:
      '2개 이상 단체 예약은 마스터 객실(대표 객실)이 필수입니다. 미지정 시 객실료가 분리 부과되어 마감이 어색해집니다.',
    keywords: ['단체예약', '마스터객실', '대표객실', '마감', '객실료'],
    sourceHash: 'be8de2e9',
    buildBody: () => `## 증상

단체 예약(객실 2개 이상)의 마감 처리 시 객실료가 **각 객실별로 따로 부과**되어, 한 ACCT#로 묶인 청구서가 나오지 않습니다. 결제·정산 과정에서 객실마다 별도 잔액이 잡혀 혼란이 생깁니다.

## 원인

1. **마스터 객실(대표 객실) 미지정** — 단체 예약 건은 마스터 객실이 하나 지정돼야 합니다. 마감 시 마스터 객실로 객실료가 일괄 부과되어 통합 청구가 가능합니다. 지정하지 않으면 각 객실로 분리 부과됩니다.

## 해결 단계

1. **체크인/아웃 화면 진입** — PMS > 객실관리 > 체크인/아웃

2. **객실 정보 탭에서 M 체크박스 확인** — 객실 리스트의 \`M\` 컬럼을 봅니다.

3. **대표 객실에 M 체크** — 마스터로 지정할 객실의 M 체크박스를 켭니다. 다른 객실은 자동으로 해제됩니다.

4. **저장** — 변경 사항을 저장하면 이후 마감부터 마스터 객실로 일괄 부과됩니다.

## 그래도 안 되면

- M 체크박스가 안 보이는 경우 → 객실 정보 탭 컬럼 설정 확인, 또는 AS 접수로 문의
- 이미 마감되어 분리 부과된 매출을 합치고 싶다면 → \`체크인/아웃 > 빌이체 처리하기\` 참고
- 단체 예약 자체를 다시 세팅하려면 → \`예약 생성·일별 요금·조식·단체 예약하기\` (howto)
`,
  },

  // ───── 신규 3) 예약조회 — 2건 ─────
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '예약조회'],
    title: '예약조회 화면과 검색 필터 구성',
    summary:
      '입실일·예약일·고객·판매채널 등 다양한 조건으로 예약을 검색하고 리스트로 확인할 수 있는 화면입니다.',
    keywords: ['예약조회', '검색필터', '입실일', '판매채널', '엑셀'],
    sourceHash: '5d98c1a9',
    buildBody: (i) => `## 개요

예약조회는 등록된 예약을 다양한 조건으로 검색하고 결과를 리스트로 확인하는 화면입니다.
고객 문의 응대, 대량 데이터 추출(엑셀), 빠른 체크인 진입 등에 사용합니다.

![화면 구성](${i[1]})

## 위치(메뉴 경로)

PMS > 객실관리 > 예약조회

## 항목 설명

| 영역 | 구성 |
|---|---|
| 상단 | \`초기화\`, \`조회\`, \`엑셀\` 등 버튼 |
| 중간 | 검색 필터 (5종 그룹) |
| 하단 | 검색 결과 리스트 (예약 상세) |

### 검색 필터 5종

| 그룹 | 항목 |
|---|---|
| 일자 | 입실일(도착) / 예약일 / 숙박기간 / 퇴실일(출발) / 확정일 |
| 예약 | ACCT# / 객실번호 / 객실타입 / 판매유형 / 상태값 |
| 판매채널 | 거래처 / OTA 예약번호 / 단체구분 |
| 고객 | 고객명 / 그룹명 / 고객구분 / 국적 / 차량번호 |
| 제외 조건 | 체크인내역 제외 / 취소·노쇼 제외 / 예약대기 제외 |

![검색 필터 상세](${i[2]})

### 결과 정렬·내보내기

- 컬럼 헤더 클릭 시 오름차순/내림차순 토글
- 우측 상단 \`엑셀\` 버튼으로 다운로드
- 결과 더블클릭 → \`예약등록\` 화면 진입

> 초기 검색 조건 기본값(예약일자/입실일자/숙박기간/퇴실일)은 \`AS문의\` 등록을 통해 변경 가능합니다.

## 관련 문서

- 검색 필터로 예약 찾고 빠르게 체크인하기 (howto)
- 예약등록 — 더블클릭 시 진입
- 체크인/아웃 — 체크인 버튼 누르면 자동 전환
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '예약조회'],
    title: '검색 필터로 예약 찾고 빠르게 체크인하기',
    summary:
      '검색 필터로 원하는 예약을 찾고, 더블클릭으로 예약등록 진입 → 체크인 버튼으로 객실 배정까지 한 번에.',
    keywords: ['검색', '예약조회', '체크인', '객실배정', '엑셀'],
    sourceHash: '5d98c1a9',
    buildBody: (i) => `## 목표

검색 필터로 원하는 예약 건을 찾고, 그 자리에서 더블클릭 → 체크인 → 객실 배정까지 한 번에 마칩니다.

## 사전 준비

- 영업일자가 정상으로 설정돼 있을 것
- 메뉴 경로: PMS > 객실관리 > 예약조회

## 단계

1. **검색 조건 입력** — 가장 자주 쓰는 조합:

   - \`입실일\`로 오늘 날짜 → 오늘 체크인 예정 건 확인
   - \`고객명\` 또는 \`ACCT#\` → 특정 고객 조회
   - \`판매채널 > 거래처\` → 채널별 일괄 조회

   ![검색 필터](${i[2]})

2. **조회 클릭** — 우측 상단 \`조회\` 버튼.

3. **결과 정렬·필터** — 컬럼 헤더 클릭으로 정렬, \`엑셀\` 버튼으로 다운로드 가능.

4. **예약 더블클릭** → \`예약등록\` 메뉴로 자동 이동합니다.

5. **체크인 처리** — \`체크인\` 버튼 클릭 → \`체크인/아웃\` 메뉴로 자동 전환.

   - 사전배정된 경우: \`저장\` → \`체크인\` → \`저장\`
   - 미배정인 경우: 객실 칸 돋보기로 객실 선택 → \`저장\`

   > 영업일자와 입실일이 동일해야 체크인 가능합니다.

## 다음 단계

- 예약금이 있는 경우 → \`예약금 등록·처리하기\` (howto)
- 매출/입금 등록·체크아웃까지 한 번에 → \`체크인/아웃 처리하기\` (howto)
- 단체 예약이 마스터 객실 미설정인 경우 → \`단체 예약 마감 객실별 분리 시\` (troubleshoot)
`,
  },

  // ───── 신규 4) 예약 캘린더 — 2건 ─────
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '예약 캘린더'],
    title: '예약 캘린더 화면 구성',
    summary:
      '월 단위 예약 현황을 캘린더로 한눈에 보고, 일자·예약 클릭으로 상세 정보를 즉시 확인하는 화면입니다.',
    keywords: ['예약캘린더', '월별조회', '필터', '인쇄', '상세예약'],
    sourceHash: '5d4e96c9',
    buildBody: (i) => `## 개요

예약 캘린더는 한 달 단위로 예약 현황을 캘린더 형태로 표시하는 화면입니다.
\`객실현황(이달)\`이 객실 가동률 중심이라면, 예약 캘린더는 예약 건수와 인원 중심으로 일정 분포를 확인합니다.

![화면 구성](${i[1]})

## 위치(메뉴 경로)

PMS > 객실관리 > 예약 캘린더

## 항목 설명

| 영역 | 구성 |
|---|---|
| 상단 버튼 | \`초기화\` / \`조회\` / \`출력\` |
| 조회 필터 | 일자 / 체크인내역 제외 / 취소·노쇼 제외 |
| 캘린더 영역 | 일자별 예약 리스트 (취소·노쇼는 빨간색) |
| 하단 상세 | 선택한 일자·예약의 상세 정보 |

### 상단 버튼

- **초기화** — 입력된 조건을 기본값으로 되돌림
- **조회** — 해당 월의 예약 건을 모두 표시
- **출력** — 캘린더 인쇄

### 조회 필터

- **일자** — 기본값 \`영업일자\`. 해당 월의 예약 조회
- **체크인내역 제외** — 이미 체크인한 건 숨김
- **취소/노쇼 제외** — 취소·노쇼 건 숨김 (체크 안 하면 **빨간색**으로 표시)

### 상세 예약 리스트

![상세 예약 리스트](${i[2]})

- 예약 기본 정보 + 해당 날짜 합계 금액
- 도착 시간은 선택한 날짜에 체크인하는 건에만 표시
- 예약 클릭 시 \`예약등록\` 메뉴로 이동

## 관련 문서

- 월별 예약·투숙 인원 캘린더로 파악하기 (howto)
- 객실현황(이달) 화면 구성 (feature)
- 예약등록 — 상세 클릭 시 이동
- 예약조회 — 조건 검색
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '예약 캘린더'],
    title: '월별 예약·투숙 인원 캘린더로 파악하기',
    summary:
      '월 단위 예약 분포와 투숙 인원을 캘린더 형태로 조회하고, 일자별 상세를 빠르게 확인하는 방법입니다.',
    keywords: ['월별예약', '인원파악', '캘린더', '조회', '인쇄'],
    sourceHash: '5d4e96c9',
    buildBody: (i) => `## 목표

한 달 단위로 예약 분포와 투숙 인원을 캘린더로 확인하고, 특정 일자의 상세 예약 정보까지 한 화면에서 파악합니다.

## 사전 준비

- 메뉴 경로: PMS > 객실관리 > 예약 캘린더

## 단계

1. **조회 일자 설정** — 기본값은 \`영업일자\` 기준의 이번 달. 다른 월을 보려면 일자 변경.

2. **필터 적용** — 상황에 맞게 선택:

   | 옵션 | 효과 |
   |---|---|
   | 체크인내역 제외 | 이미 체크인한 예약 숨김 (앞으로 들어올 예약만) |
   | 취소/노쇼 제외 | 취소·노쇼 건 숨김 (체크 안 하면 빨간색으로 표시) |

3. **조회 클릭** — 캘린더에 일별 예약 리스트가 표시됩니다.

   ![캘린더 화면](${i[1]})

4. **일자/예약 클릭** — 하단 상세 예약 리스트에 해당 일자의 정보가 간략 표시됩니다.

   - 예약 기본 정보 + 그 날 발생하는 합계 금액
   - 도착 시간 (체크인하는 건에 한해)

5. **상세 진입** — 상세 리스트의 예약을 클릭하면 \`예약등록\` 메뉴로 이동, 전체 정보를 조회·수정할 수 있습니다.

6. **인쇄 (선택)** — 캘린더를 종이로 출력하려면 상단 \`출력\` 버튼.

## 다음 단계

- 특정 조건으로 예약을 찾고 싶다면 → \`예약조회\` (howto)
- 객실 가동률 중심 캘린더가 필요하다면 → \`객실현황(이달)\` (feature/howto)
- 예약을 새로 만들려면 → \`예약등록\` (howto)
`,
  },

  // ───── 신규 5) 체크인/아웃 — 3건 ─────
  {
    productCode: 'pms',
    contentType: 'feature',
    categoryPath: ['객실관리', '체크인/아웃'],
    title: '체크인/아웃 화면과 빌이체 기능 개요',
    summary:
      '체크인·체크아웃 처리, 매출/입금 등록, 빌이체까지 객실 운영의 정산 흐름을 모두 다루는 화면입니다.',
    keywords: ['체크인', '체크아웃', '매출', '입금', '빌이체'],
    sourceHash: 'cac9930d',
    buildBody: (i) => `## 개요

체크인/아웃은 예약 건의 입·퇴실 처리부터 매출·입금 등록, 빌이체까지 정산의 모든 흐름을 다루는 화면입니다.
프론트 데스크의 결제·청구 핵심 화면이며 \`미결산 내역\`·\`일마감\`과 직접 연계됩니다.

![화면 구성](${i[1]})

## 위치(메뉴 경로)

PMS > 객실관리 > 체크인/아웃

## 항목 설명

| 영역 | 구성 |
|---|---|
| 상단 | \`예약체크인\`, \`전체체크아웃\`, \`매출\`, \`입금\`, \`빌이체\` 버튼 |
| 중간 | 예약정보 / 연락처 / 거래처 정보 |
| 하단 | \`객실정보\` 탭 / \`입출금\` 탭 |

### 빌이체 (Bill Transfer)

다른 객실로 결제·청구를 이전하는 기능입니다.

![빌이체 화면](${i[5]})

- **동일 ACCT# 내** — 단체 예약의 객실 간 결제 이전
- **타 ACCT#** — 다른 예약 건으로 결제 이전

### 빌이체 팝업 버튼

| 버튼 | 동작 |
|---|---|
| 초기화 | 수정 내역 되돌리기 |
| 전체선택 | 보내는 쪽 Bill# 모두 선택 |
| 선택이동 | 클릭한 빌을 옮기기 |
| 선택취소 | 이동한 빌을 취소 |
| 저장 | 변경 내역 저장 |

## 관련 문서

- 체크인·체크아웃·매출/입금 처리하기 (howto)
- 빌이체 처리하기 (howto)
- 예약조회 — 체크인 버튼으로 이동
- 미결산 내역 — 객실 매출/입금 일괄 확인
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '체크인/아웃'],
    title: '체크인·체크아웃·매출/입금 처리하기',
    summary:
      '예약 체크인부터 매출/입금 등록·취소, 체크아웃까지 객실 정산의 표준 흐름을 단계별로 안내합니다.',
    keywords: ['체크인', '체크아웃', '매출', '입금', '잔액'],
    sourceHash: 'cac9930d',
    buildBody: (i) => `## 목표

예약 건을 체크인 → 객실 사용 중 매출/입금 등록 → 잔액 0 확인 → 체크아웃까지 표준 정산 흐름을 마칩니다.

## 사전 준비

- \`예약확정\` 또는 사전 체크인 가능한 예약 건 존재
- 영업일자 정상 설정
- 메뉴 경로: PMS > 객실관리 > 체크인/아웃

## 단계

1. **체크인 처리** — 상단 \`예약체크인\` 버튼.

   - 예약체크인 창에서 체크인할 건을 **더블클릭**
   - 객실 번호 사전 배정: \`저장\` 클릭
   - 객실 번호 미배정: 객실을 배정한 뒤 \`저장\`

   ![체크인 화면](${i[2]})

2. **매출 등록** — \`입출금\` 탭 → \`매출\` 버튼.

   - 구분 선택 (ROOM / Early Check-In 등)
   - 판매가 입력 → \`저장\`

   ![매출 등록](${i[3]})

3. **입금 등록** — \`입출금\` 탭 → \`입금\` 버튼.

   - 구분 선택 (CARD / CASH 등)
   - 금액 입력 → \`저장\`

   ![입금 등록](${i[4]})

4. **매출/입금 취소** — 취소할 항목을 우클릭 → \`매출/입금 취소\` → 사유 선택 → \`저장\`.

5. **체크아웃 처리** — 다음 2가지 조건을 먼저 확인합니다.

   - 좌측 상단 \`영업일자\`와 리스트의 \`퇴실일\`이 동일
   - \`입출금\` 탭에서 잔액 = 0

   조건 충족 시 다음 방법 중 하나:

   - 리스트의 \`상태\` 칸을 \`체크아웃\`으로 변경
   - 우측 상단 \`전체체크아웃\` 버튼

   체크아웃되면 \`객실현황\`에서 **공실 / Dirty**로 표시됩니다.

## 다음 단계

- 객실 매출/입금을 한눈에 확인하려면 → \`미결산 내역\`
- 결제를 다른 객실로 이전하려면 → \`빌이체 처리하기\` (howto)
- 일자 마감 → \`객실 일마감 > 일마감 사전체크\`

## 주의사항

| # | 조건 | 안 됐을 때 |
|---|---|---|
| 1 | 영업일자 = 퇴실일 | 체크아웃 불가 |
| 2 | 매출/입금 잔액 = 0 | 체크아웃 불가 |
| 3 | 체크아웃 후 | 객실현황에 \`공실 / Dirty\` 표시 |

## 체크인 예정 예약을 빨리 보는 3가지 경로

1. \`실시간 객실(오늘)\` → \`예약확정\` 상태 체크
2. \`객실현황(이달)\` → \`TODAY\` 버튼
3. \`체크인/아웃\` → \`예약체크인\` 버튼
`,
  },
  {
    productCode: 'pms',
    contentType: 'howto',
    categoryPath: ['객실관리', '체크인/아웃'],
    title: '빌이체 처리하기 (동일 ACCT# / 타 ACCT#)',
    summary:
      '다른 객실에서 결제를 대신 처리해야 할 때 빌이체(Bill Transfer)로 결제·청구를 이전하는 방법입니다.',
    keywords: ['빌이체', 'ACCT', '청구이전', '단체결제', '대납'],
    sourceHash: 'cac9930d',
    buildBody: (i) => `## 목표

체크인된 예약 건의 결제·청구를 다른 객실로 이전합니다. 단체 결제, 대납, 체크아웃이 막힌 객실의 잔액 처리 등에 사용합니다.

## 사전 준비

- **체크인 완료된 예약 건** (이체 보내는·받는 양쪽)
- 메뉴 경로: PMS > 객실관리 > 체크인/아웃

## 단계

### 동일 ACCT# 내 빌이체

1. **예약 건 선택** — 체크인된 예약 건 선택.

2. **입출금 탭 → 빌이체 버튼** — 우측 상단 \`빌이체\` 클릭.

   ![빌이체 화면](${i[5]})

3. **양쪽 내역 확인** — 보내는 쪽·받는 쪽의 입출금 내역이 자동 표시됩니다.

   ![보내는 쪽 내역](${i[6]})

   ![받는 쪽 내역](${i[7]})

4. **빌 선택 이동** — 이체할 빌을 클릭 → \`선택이동\` → \`저장\`.

### 타 ACCT# 빌이체

1. **빌이체 창에서 받는쪽 ACCT# 기입** — 받을 객실의 ACCT# 입력 → 해당 빌 불러옵니다.

   ![타 ACCT# 빌이체](${i[8]})

   > ACCT#가 생각나지 않으면 돋보기 클릭으로 조회.

2. **빌 선택 이동** — \`선택이동\` → \`저장\`.

## 다음 단계

- 이체 받은 객실의 잔액 확인 → \`체크인/아웃\` 입출금 탭
- 체크아웃 시 잔액 0 처리 → \`체크인·체크아웃·매출/입금 처리하기\` (howto)
- 객실 일괄 마감 → \`객실 일마감\`

## 빌이체 팝업 버튼 정리

| 버튼 | 동작 |
|---|---|
| 초기화 | 수정 내역 되돌리기 |
| 전체선택 | 보내는 쪽 Bill# 모두 선택 |
| 선택이동 | 클릭한 빌을 받는 쪽으로 이동 |
| 선택취소 | 이동한 빌을 보내는 쪽으로 되돌림 |
| 저장 | 변경 내역 저장 |
`,
  },
];
